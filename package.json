{
  "name": "react-native-version-check",
  "version": "1.0.0",
  "description": "Check google playstore and apple appstore bundle versions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react-native-bundle-check"
  ],
  "author": "tom bannister",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0"
  }
}
